# ML API 📡

This folder contains the backend API for the Tarass project, which serves the machine learning models.

---

## ⚡ Note
The full API folder is available as a compressed file due to its size.  
You can download it here:  
[Download ML API ZIP](https://drive.google.com/file/d/1GWRysFNddcsoMgm8CS-HsxmgGq-0CgPb/view?usp=drive_link)

---

## 🚀 Getting Started
After downloading and extracting the ZIP:

1. Make sure you have Python installed (version 3.8+ recommended).
2. Install required libraries:
    ```bash
    pip install -r requirements.txt
    ```
3. Run the API:
    ```bash
    python app.py
    ```
4. The API endpoints will be available at `http://localhost:5000` (or the port defined in `app.py`).

---

## 🛠️ Folder Contents
- `app.py` : Main API script to run the backend
- `requirements.txt` : List of Python libraries needed
- Other files/folders: supporting scripts for serving the ML models

